# Clase 2 de Estadística de Áreas

Midiendo asociación, los residuales explican dos variables pero no la distancia inversa.

Autocorrelación positica reiduaoes ifuales
auto neg residules cerca son diferentes

expetetion in imoran es el valor esperado. se reclaza la h0 entre dependencia de ubicaciones, por tanto hay  autocorrelación

Las Wij son matrices de cero por distancia o ceros y unos (ejemplo de las suma de los vecinos de cada locación)


El lambda describe la autocorrelación espacial el phi


El AIC el punto más importante par comorar modelos espafiales

sino hay autocorrelación todo el mapa es cero, se divide en sar la ecuación en dos componentes, el de tendencia y el correlación.

si incluyo una variable que incluya la correlacón espacial hago que la autocorrelación no sea dependiente es decir no haya autocorrelación.

lambda me interesa que p<0.05

el w es lo que hace la diferencias.

La matrix dispersa que la dependencia esta en bloques o que solo las primeras 3 lineas tienen información, el resto es cero. 